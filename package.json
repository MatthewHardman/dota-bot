{
  "name": "dota-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "update": "npx npm-check-updates --upgrade && echo 'Running `npm install`...' && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.21.0",
    "openai": "^3.2.1"
  },
  "devDependencies": {
    "npm-check-updates": "^18.0.1"
  }
}
